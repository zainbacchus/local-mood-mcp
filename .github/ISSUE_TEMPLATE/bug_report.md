---
name: Bug report
about: Something behaves incorrectly
labels: bug
---

**What happened?**

**What did you expect?**

**Memory state** — this changes most behavior, please fill in:

- [ ] API-only (no export imported)
- [ ] Extended Streaming History imported (drop folder or custom path?)

Output of `extended_history_status` and `library_stats`, if relevant:

```
(paste here — these contain no secrets)
```

**Environment**

- OS:
- Python version:
- Installed how (clone + `pip install -e .` / other):

**Steps to reproduce**

1.
